export function DisclaimerBanner() {
  return (
    <div className="w-full bg-amber-50 border-b border-amber-200 text-amber-900 text-xs sm:text-sm px-4 py-2 text-center">
      <span className="font-semibold">Educational demonstration</span> — not a
      medical device, no real patient data is used, and the generated scores
      are not clinically validated.
    </div>
  );
}
