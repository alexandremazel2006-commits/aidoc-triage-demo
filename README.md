# Interactive demo — Aidoc, AI radiology triage

Two educational, academic-case-study demos live in this repo:

1. **[Aidoc worklist demo](#aidoc-worklist-demo)** (below) — a Next.js app illustrating how
   Aidoc's AI *reorders* a radiology worklist by urgency, using Claude for the reasoning.
2. **[RadioCheck AI](#radiocheck-ai)** — a real, locally-run chest X-ray *analysis* tool
   (Next.js frontend + Python/FastAPI backend running an actual pretrained medical CV
   model — no LLM, no API key) with Grad-CAM explainability and a draft report generator.

Neither is a certified medical device. See each section's disclaimer.

## Aidoc worklist demo

Educational, fictional demonstration of how [Aidoc](https://www.aidoc.com), an AI radiology
triage system, works — built for an oral presentation / academic case study.

> ⚠️ **This is not a medical device.** No real patient case data is used — the fictional
> cases, scores, and clinical contexts shown are made up for illustration. The radiology
> photos are real, but are anonymized, openly-licensed teaching images unrelated to any of
> the fictional cases (see Credits below). Scores are not clinically validated.

## The concept illustrated

- Radiologists normally work through scans in the order they arrive (FIFO), with no signal
  of clinical urgency.
- Aidoc analyzes images in the background and, when it detects a sign of severity, moves the
  case to the top of the worklist — **it never makes a diagnosis**.
- The radiologist remains solely responsible for reading and signing the report: the AI
  changes *when* a case is seen, never *who* is responsible.

This demo shows a worklist of 13 fictional cases, before (arrival order) and after (sorted
by an urgency score generated by Claude), with a "human-in-the-loop" detail panel where the
radiologist confirms or rejects the proposed priority — plus a form to add your own case to
the worklist before running the triage.

## Tech stack

- [Next.js](https://nextjs.org) (App Router) + TypeScript + Tailwind CSS
- [Anthropic Claude API](https://docs.claude.com) called only server-side
  (`src/app/api/analyze/route.ts`) via a Next.js API route — the API key is never exposed to
  the client
- Real, openly-licensed radiology photos (normal anatomy, no visible pathology) from
  Wikimedia Commons, with an SVG silhouette fallback for cases added without a photo

## Running locally

### Prerequisites

- Node.js 18.18+ (tested with Node 26)
- An Anthropic API key: [console.anthropic.com/settings/keys](https://console.anthropic.com/settings/keys)

### Install

```bash
npm install
```

### Environment variables

Copy `.env.example` to `.env.local` (already gitignored) and fill in your key:

```bash
cp .env.example .env.local
```

```
ANTHROPIC_API_KEY=...
```

### Development

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The home page explains the concept;
`/worklist` has the interactive demo ("Run AI triage" button, "+ Add a case" form).

### Production build

```bash
npm run build
npm run start
```

## Project structure

```
src/
├── app/
│   ├── layout.tsx          # root layout + persistent disclaimer banner
│   ├── page.tsx             # home page: Aidoc background, stats, philosophy
│   ├── worklist/page.tsx    # main before/after demo
│   └── api/analyze/route.ts # server call to Claude (never from the client)
├── components/               # UI (worklist, detail panel, badges, images, nav, add-case form)
└── lib/
    ├── cases.ts              # 13 fictional cases (static dataset)
    ├── claude.ts              # Anthropic client + structured output (Zod)
    ├── prompts.ts             # triage system prompt (prioritization tool, not diagnosis)
    └── types.ts               # shared types
```

## Deploying to Vercel

See the step-by-step instructions provided separately, or in short:

1. Push this repo to GitHub.
2. On [vercel.com/new](https://vercel.com/new), import the repo.
3. Add the `ANTHROPIC_API_KEY` environment variable under Project Settings →
   Environment Variables (secret value, never committed).
4. Deploy — Vercel auto-detects Next.js.

## RadioCheck AI

> ⚠️ Research & educational use only — not intended for clinical diagnosis. University
> prototype, not a certified medical device.

A second, more ambitious demo living in the same repo: upload a chest X-ray and a real,
locally-run pretrained model ([TorchXRayVision](https://github.com/mlmed/torchxrayvision))
scores it across 18 pathologies — no LLM, no API key, works fully offline after the one-time
model download. Includes a real Grad-CAM explainability heatmap, a deterministic (non-LLM)
draft report generator, a radiologist review workflow, and a persisted history/queue/
dashboard — all backed by a Python/FastAPI service with its own SQLite database.

Full details (architecture, exact model/dataset provenance, setup, troubleshooting) live in
**[`backend/README.md`](backend/README.md)**. Quick start:

```bash
# Terminal 1 — backend
cd backend
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python test_model.py tests/fixtures/sample_chest_xray.png   # sanity check first
uvicorn app.main:app --reload --port 8000

# Terminal 2 — frontend (repo root, same Next.js app as the Aidoc demo)
npm install
npm run dev
```

Then open [http://localhost:3000/radiocheck](http://localhost:3000/radiocheck).

### Pages

| Route | Purpose |
|---|---|
| `/radiocheck` | Landing page |
| `/analyze` | Upload a chest X-ray, run the model, see results |
| `/exams/[id]` | Full record: image, findings, Grad-CAM, draft report, review |
| `/dashboard` | Live stats + recent examinations, computed from the real DB |
| `/queue` | Worklist sorted by demo priority, with filters |
| `/history` | Search/filter all saved analyses |
| `/about-ai` | Model, datasets, supported pathologies, and limitations |

### What's real vs. simulated

| | |
|---|---|
| Model inference | **Real** — actual DenseNet121 forward pass, no mocked scores |
| Grad-CAM heatmap | **Real** — computed from the model's own gradients, class-discriminative |
| Priority (CRITICAL/HIGH/MEDIUM/LOW) | Simulated — demo-only thresholds, see `priority_engine.py` |
| Draft report | Deterministic template from real scores — not an LLM, no invented findings |
| "Abnormal" demo examples | None bundled — see `/about-ai` and the Demo Mode note below |

### Backend tests

```bash
cd backend && source venv/bin/activate && python -m pytest -v
```

Real integration tests (real model, temporary SQLite) — see `backend/README.md`.

No real patient images are used here either; the model is a general-purpose open-source
checkpoint, not fine-tuned on anything in this repo.

## Credits

No real patient images are used. The 13 case photos are real, anonymized radiology teaching
images under an open license, sourced from Wikimedia Commons and resized for the web. They
depict normal anatomy (no visible pathology) and are not linked to any of this demo's
fictional clinical scenarios.

| File | Source | Author | License |
|---|---|---|---|
| c1.jpg | [Scrollable CT of a normal thorax](https://commons.wikimedia.org/wiki/Scrollable_high-resolution_computed_tomography_images_of_a_normal_thorax) | Mikael Häggström, M.D. | CC0 |
| c2.png | [CT of a normal brain, axial 10](https://commons.wikimedia.org/wiki/File:CT_of_a_normal_brain,_axial_10.png) | Mikael Häggström, M.D. | CC0 |
| c3.jpg | [X-ray of normal wrist, lateral](https://commons.wikimedia.org/wiki/File:X-ray_of_normal_wrist_by_lateral_projection.jpg) | Mikael Häggström, M.D. | CC0 |
| c4.png | [Chest Xray PA 3-8-2010](https://commons.wikimedia.org/wiki/File:Chest_Xray_PA_3-8-2010.png) | Stillwaterising | CC0 |
| c5.png | [CT of a normal brain (thumbnail)](https://commons.wikimedia.org/wiki/File:CT_of_a_normal_brain_(thumbnail).png) | Mikael Häggström, M.D. | CC0 |
| c6.png | [CT of a normal abdomen and pelvis, axial 94](https://commons.wikimedia.org/wiki/File:CT_of_a_normal_abdomen_and_pelvis,_axial_plane_94.png) | Mikael Häggström, M.D. | CC0 |
| c7.jpg | [X-ray of the cervical spine, lateral](https://commons.wikimedia.org/wiki/File:X-ray_of_the_cervical_spine_of_a_20_year_old_male_-_lateral.jpg) | Bonepit Collection, UC San Diego | Public Domain |
| c8.jpg | [Scrollable CT of a normal thorax](https://commons.wikimedia.org/wiki/Scrollable_high-resolution_computed_tomography_images_of_a_normal_thorax) | Mikael Häggström, M.D. | CC0 |
| c9.jpg | [X-ray of normal ankle, lateral](https://commons.wikimedia.org/wiki/File:X-ray_of_normal_ankle_-_lateral.jpg) | Mikael Häggström, M.D. | CC0 |
| c10.jpg | [Head CT scan](https://commons.wikimedia.org/wiki/File:Head_CT_scan.jpg) | Andrew Ciscel | CC BY-SA 2.0 |
| c11.png | [CT of a normal abdomen and pelvis, axial 22](https://commons.wikimedia.org/wiki/File:CT_of_a_normal_abdomen_and_pelvis,_axial_plane_22.png) | Mikael Häggström, M.D. | CC0 |
| c12.jpg | [Normal PA chest radiograph](https://commons.wikimedia.org/wiki/File:Normal_posteroanterior_(PA)_chest_radiograph_(X-ray).jpg) | Mikael Häggström, M.D. | CC0 |
| c13.jpg | [CT Brain Scan](https://commons.wikimedia.org/wiki/File:CT_Brain_Scan.jpg) | Goleisureintl | CC BY 4.0 |

Files are in `public/images/cases/`. CC BY-SA / CC BY entries (c10, c13) require attribution,
kept here and in-app next to the case.
